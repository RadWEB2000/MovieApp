import styled from 'styled-components';

export const AboutSectionTitle = styled.h2`
    color:var(--redDark);
    font-size:5rem;
    padding-left:1rem;

    &::first-letter{
        color:var(--red);
    }
`