import styled from 'styled-components';

export const FavouritesTitle = styled.h2`
    color:var(--red);
    font-size:3.5rem;
    margin:3rem 0;
    text-align:center;
    text-transform:uppercase;
`