import styled from 'styled-components';

export const FavouritesTitle = styled.h2`
    text-align:center;
    font-size:3.5rem;
    color:var(--red);
    text-transform:uppercase;
    margin:3rem 0;
`