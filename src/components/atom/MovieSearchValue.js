import styled from 'styled-components';

export const MovieSearchValue = styled.h1`
    color:var(--white);
    font-style:italic;
    font-size:3rem;
    margin:.75rem 0;
    max-width:100vw;
    overflow-x:hidden;
    text-align:center;
    -webkit-text-stroke:.03rem var(--grey);
`